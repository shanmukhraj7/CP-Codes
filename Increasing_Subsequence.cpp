#include <bits/stdc++.h>
using namespace std;

// const ll MOD = 1e9 + 7;
// const ll INF = 1e18;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);

    int n;
    cin >> n;
    vector<int> a(n);
    for(auto x : a) cin >> x;
    vector<int> res;
    for()

    return 0;
}